#include <stdio.h>

int main() {
int n;
    printf("Bir sayi giriniz: ");
    scanf("%d", &n);

    for (int i = 1; i <= n; i++) {
        if (i > 50) break;
        if (i % 3 == 0) continue;
        printf("%d\n", i);
    }
}
